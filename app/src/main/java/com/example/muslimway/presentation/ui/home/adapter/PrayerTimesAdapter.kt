package com.example.muslimway.presentation.ui.home.adapter

import android.view.LayoutInflater
import android.view.View
import android.view.ViewGroup
import androidx.databinding.DataBindingUtil
import androidx.recyclerview.widget.RecyclerView
import com.example.muslimway.R
import com.example.muslimway.databinding.PrayerTimeLayoutBinding

/*
class PrayerTimesAdapter : RecyclerView.Adapter<PrayerTimesAdapter.PrayerTimesViewHolder>() {

    private val newsList = mutableListOf<DayOfMonth>()

    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): PrayerTimesViewHolder {
        val inflater = LayoutInflater.from(parent.context)
        return PrayerTimesViewHolder(
            DataBindingUtil.inflate(
                inflater,
                R.layout.prayer_time_layout,
                parent,
                false
            )
        )
    }


    override fun onBindViewHolder(holder: PrayerTimesViewHolder, position: Int) {
        holder.bind(newsList ])
    }

    override fun getItemCount() = newsList.size

    fun setData(newsList: List<DayOfMonth>) {
        this.newsList.addAll(newsList)
        notifyDataSetChanged()
    }

    inner class PrayerTimesViewHolder(private val binding: PrayerTimeLayoutBinding)
        : RecyclerView.ViewHolder(binding.root) {

         fun bind(prayerTime: Timings) {

        }
    }


}*/
